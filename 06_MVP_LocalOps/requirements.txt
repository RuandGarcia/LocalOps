# LocalOps — pipeline preditivo (ETL, forecast, classificadores, SHAP, recomendador, API, agente)
# Testado com Python 3.11+. Se estiver em Windows com Python 3.13, os mesmos
# pacotes funcionam; ajuste as versões com "pip install -U <pacote>" se algum
# wheel específico não estiver disponível ainda para a sua versão do Python.

pandas>=2.2
numpy>=1.26
openpyxl>=3.1
pyarrow>=15.0

prophet>=1.1
xgboost>=2.0
scikit-learn>=1.4
shap>=0.45
holidays>=0.45
joblib>=1.3

fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

matplotlib>=3.8   # usado só pelo localops/diagnostics.py (gráficos de diagnóstico)
plotly>=5.20       # usado pelo dashboard/ e pelos gráficos de localops/agent.py

# Dashboard
streamlit>=1.32

# Agente IA online (Gemini, camada gratuita) — opcional: sem GEMINI_API_KEY
# configurada, o agente cai automaticamente no motor de regras offline.
google-genai>=1.0
